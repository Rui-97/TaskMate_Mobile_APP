import { View, SafeAreaView, Text } from "react-native";

const PomoCompleteScreen = () => {
  return (
    <SafeAreaView>
      <Text>PomoComplete Screen</Text>
    </SafeAreaView>
  );
};

export default PomoCompleteScreen;
