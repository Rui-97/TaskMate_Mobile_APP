import { View, SafeAreaView, Text } from "react-native";

const CalendarWeeklyScreen = () => {
  return (
    <SafeAreaView>
      <Text>CalendarWeekly Screen</Text>
    </SafeAreaView>
  );
};

export default CalendarWeeklyScreen;
