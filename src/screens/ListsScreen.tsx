import { View, SafeAreaView, Text } from "react-native";

const ListsScreen = () => {
  return (
    <SafeAreaView>
      <Text>Lists Screen</Text>
    </SafeAreaView>
  );
};

export default ListsScreen;
