import { View, Text, StyleSheet } from "react-native";

const AlternativeOptionRow = () => {
  return (
    <View>
      <Text></Text>
    </View>
  );
};

export default AlternativeOptionRow;
const styles = StyleSheet.create({
  container: {
    flexDirection: "row",
  },
});
